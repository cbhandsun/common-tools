# 组件重建接线修复与验收

## 后续误判及正文丢失修复

2026-09-07 后续排查确认三处独立问题：矩阵组件中的装饰性 ring-node 被误当作同心圆证据；细碎图标像素被整体判作截图，遮住了可编辑正文；解除截图误判后，这些像素又被用于虚构流程卡片和连线。现在要求截图具有重复对齐行，对满足颜色、密度及相邻长线条件但面积不足区域 2% 的散点标记原生几何未确认，阻止流程骨架、visual atom 和模板重建，保留原始图形及可编辑文字。

实际回放进一步发现模板文字替换的所有权错误：模板产物后来被丢弃，却提前消费了 4 个原始 OCR 文本框。未确认区域现在在模板生成入口就被拒绝，即便匹配到了可信本地模板也不能绕过；另增加生产断言，已经擦除文字的该类区域若丢失已准入正文，则直接失败，错误消息不包含用户内容。

使用已缓存的真实 OCR 运行相同生产 Worker composition，源 PDF 第 2 页最终保留全部 17 个文本框、0 个虚构原生形状。人工检查确认上排四行正文恢复，错误方框、圆形及连线消失。像素差异比例 0.06437，前景缺失比例 0.02052，视觉检查通过。此前临时结果虽有 45 个形状，却明显破坏画面；形状计数不能作为成功证明。图标、卡片背景及原图连接线仍为保真图片，这不是全原生重建完成。

111 项定向回归测试通过（99 项几何、截图与组件测试，12 项 Worker 及图像质量测试）；ESLint、类型、架构预算和工作区边界通过，新增测试由统一 unit CI 自动发现。未重跑完整 CI。主组件文件字节预算降低到 233656。

干净服务镜像 `common-tools-image-to-editable-paddleocr:semantic-guard-fixed-20260907`，摘要 `6b7e508501ff11a54a499335d9387dc543fcfbdd880521202ae08ab4bb6653c7`；入口及镜像均不含临时 semantic-probe。独立回放文件位于 `output/pdf-validation-20260907/semantic-fix/`。最终两页 MCP 任务为 `244e7b0b-43dd-4da1-ac9f-7198ef16fa36`，其端到端结果见下方后续记录。

端到端最终结果：任务 `244e7b0b-43dd-4da1-ac9f-7198ef16fa36` 为 `failed / QUALITY_GATE_FAILED`。实际 IR 第 2 页为 17 文本框、0 原生形状，第 6 页为 19 文本框、13 原生形状，合计 36 文本框、13 形状、11 连接线、4 保真图片。视觉相似度、生成、多格式一致性及残留去重检查通过；原生图形、原生组件质量、复杂图形原生化检查未通过，不能宣称恢复此前基本全原生的效果。最终文件为 `output/pdf-validation-20260907/semantic-fix/两页_修复后_诊断版.pptx`，完整门禁记录为同目录 `verification.json`。

验证后服务资源恢复至 2 CPU、3 GiB 内存、内存与交换空间共 6 GiB；基础与 PaddleOCR 镜像 local 标签同步到本次干净版本，旧镜像和容器保留以便回滚。未再次执行整份 16 页 OCR，未修改质量门槛。

以下为本次修复前的接线验收记录。

远程 Worker 已补齐逐页预分析、已校验离线组件库匹配、策略及资产索引传递。组件库按只读方式挂载，当前可用资产为 11 个，校验后总计 400,816 字节。内部匹配保留受信路径和结构特征，对外只记录紧凑引用与来源哈希。

本轮同时修复实际集成暴露的问题：语义形状图层使用空 imageIndex 的格式兼容；无匹配时保留建议压过原生检测；组件目录丢失匹配必需字段；源文件字节数进入通用 IR 数值字段；模板学习数据重复嵌入导致结构深度超限。保留策略及仅模板建议不会强制关闭既有原生检测，模板几何在内部重建后不再重复写入公开来源记录。没有提高数值、深度、节点或质量门槛。

91 项相关测试通过，类型、定向 ESLint、工作区边界和架构预算通过；新增测试由 unit CI 自动发现。主脚本字节预算继续下降。未重跑完整 CI。生产候选基于既有干净镜像，仅复制本轮所需文件，保留旧容器和镜像用于回滚；没有提交无关工作区修改。

真实验收使用用户 PDF 的第 2、6 页，经已安装的 HTTPS MCP 上传及创建任务。最终 Job 为 `6ef734ed-c83c-42b5-beb4-2b0044caffed`，状态 `failed / QUALITY_GATE_FAILED`，有完整诊断产物。两页分别匹配 4 个候选资产，但没有新增已应用原生组件组；原生形状共 13 个、连接线 11 条、文本框 24 个、残留图片 4 张，与恢复后的原有识别结果一致。

文档规范化、视觉相似度、残留重复去除、PPTX 生成及多格式一致性通过；原生图形、原生组件质量和复杂图形原生化门禁未通过。组件匹配数不等于实际原生重建数，本轮不能宣称已恢复此前本地转换的效果，也不是完整 16 页质量验收成功。

最终干净镜像为 `common-tools-image-to-editable-paddleocr:component-evidence-fixed-20260907`，摘要 `6496bf0dbc5f99ea5555e27002ae60a47e23cc0ba5169d8a200f37baa63ea88a`。临时诊断预加载已移除，两个 PPT Worker 心跳正常；验证期间临时资源已恢复至 2 CPU、3 GiB 内存、6 GiB 内存与交换空间总额。PaddleOCR 与基础镜像的 local 标签已同步，保留回滚标签。

后续质量工作应围绕未原生化图形补充重建能力及实际组件应用证据；不继续重复整份 OCR，也不把匹配成功当作视觉或可编辑性达标。部署使用方法见 [team-component-analysis.md](team-component-analysis.md)。本地验收详情位于 `.codex-tmp/component-acceptance-evidence.json` 和 `.codex-tmp/component-acceptance.ir.json`。
