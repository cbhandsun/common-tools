# 16 页组件原生化验证记录

本轮按用户认可的第 2 页继续处理同一 PDF 的其他 15 页，保留第 2 页原有 Deck IR。复用已有结构化输入打包、OpenXML 构建、生产 Worker handler、源图渲染比较及多格式交付路径；没有新增像素识别器、修改门禁或部署远程服务。其余页由源图人工确认结构后生成 IR，不能据此声称自动识别能力已经覆盖这些页面。

## 产物与可编辑性

本地交付目录：`output/component-native-full-20260907/`。PPTX、PDF、可复现的结构化源归档 `native-source.tar.gz`、Worker 质量报告和逐页报告留在本地，不作为源码提交。

| 项目 | 实际结果 |
| --- | --- |
| 页数 | 16 |
| 原生文本框 | 217 |
| 原生形状（不含连接线） | 127 |
| 原生连接线 | 92 |
| 局部图片 | 39 |
| 原生数据表 / 数据图表 | 0 / 0；表格和图表由独立文本、形状构成 |
| 无图片页 | 6、14、15、16 |
| 最大单图面积占比 | 34.83%，第 1 页封面装饰插画 |

直接检查 PPTX ZIP：16 个 slide XML、344 个 `p:sp`、92 个 `p:cxnSp`、39 个 `p:pic` 和 39 个媒体文件。源图仅用于比较，没有作为整页图片嵌入 PPTX。

第 6 页用五段原生路径表达源图圆环，保留源文字和五个柱形；第 13 页用原生自由形状表达箭头面板，正文和连接线独立可编辑。源第 6 页的百分比标签与中心覆盖率不完全自洽，本轮原样保留文字，扇区仅复现源视觉边界，不补造数据或宣称这是可计算的数据图表。

## 实际验证结果

真实本地生产 Worker handler 已完成整份构建、16 页渲染和 PPTX/PDF 交付。结构校验、资源解析、原生图形门禁、多格式页数和源指纹一致性均通过。**整份 `visual-fidelity` 未通过，因此 Worker 总质量结果为 false，不能宣称全部页面达到第 2 页的保真度或通过生产验收。**

整份报告返回像素差异指标 0.147240、前景缺失指标 0.176766、平均绝对差异指标 19.955477；比较 16 页、475 个组件区域，其中 58 个关注区域。指标来自原有比较器，未修改阈值。逐页报告用于定位差异，最终整份结果以 `worker-result.json` 为准。

仍有较大局部插画的页面：第 4 页三层堆栈、第 5 页平台插画、第 8 页界面示意、第 12 页场景插画，以及第 1 页封面。其他视觉差异包括字体度量、渐变、发光和电路装饰。此次交付为完整可编辑复核版，不是全原生、全视觉验收通过版。

复用现有测量驱动字体校正器执行单次保守检查，没有页面产生可接受改动；没有新建字体拟合算法。本轮无生产代码变更，因此未重复运行此前已通过的全套代码回归，验证集中于实际交付文件。

- 源 PDF SHA-256：`ec982b2b53509ff69c1a488d9ca8d852d362af8f6117ff49993a5020baf1f367`
- 完整 PPTX SHA-256：`d321670b66939551b6732adfd0d1be6caf4af16cf3e5610afbe284d8ccf332df`

样本已用于调试，不能作为独立验收集。未更新已安装插件，未部署或推送远程。
