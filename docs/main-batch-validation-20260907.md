# main 分批提交验收记录

本轮把累计架构、重建与运行边界变更按核心代码、部署发布、插件文档三批提交到本地 main。未推送远端；测试输入、PPTX、PDF、截图和诊断输出不入库。

## 本次提交前验证

- Lint、类型检查、架构预算、工作区依赖边界通过。
- 插件、能力契约、可观测性配置、ADR 和运行包验证通过。
- .NET 锁定依赖安全审计与 Release 构建通过；生产 npm 依赖审计无漏洞；Python 锁定依赖的哈希校验 dry-run 通过。
- 完整执行一次 `npm run test:ci`，发现新建 PPT 候选数量检查、运行包依赖清单和旧架构测试断言的问题。修复后复跑所有失败所在测试文件；最后一项引擎测试在独立复测中通过（6/6）。其余复测通过，原有一个环境条件跳过保持原状，没有新增跳过。
- 没有在最终修改后重新执行整个全量套件，因此本记录不宣称单次全量 CI 全绿；证据是全量执行加受影响测试复测。原始日志保存在本地 `.codex-tmp/precommit-*.log`。

候选版式检查恢复原先至少两个候选的要求，同时支持显式 `variantCount: 1`；请求三个候选是选择器上限，不代表每页一定有三个兼容版式。新增测试覆盖两个候选通过、单候选不足、空候选和非法候选集合失败。

## 尚未完成

“大块图片进一步原生化”尚未完成，不能以本次提交代替质量验收。现有两页诊断结果仍未通过原生图形质量门禁。下一步应复用已有本地结构理解与 Deck IR 输入路径，并补齐相同的源图质量验证；没有新增另一套卡片识别算法，也没有发布该增强。
