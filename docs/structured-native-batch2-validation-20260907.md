# 组件原生化第二批验证

在用户认可已识别组件的效果后，继续拆解现有完整 16 页版本中的大块图片。复用现有 Deck IR、自由形状路径、文字旋转、渐变、图片裁切、OpenXML 构建器及生产 Worker handler。没有新增识别算法、修改生产代码或放宽门禁。

## 本批接受的变更

| 页 | 内容 | 图片总面积占比变化 |
| --- | --- | --- |
| 4 | 三层立体堆栈拆为原生面、标签、电路线和连接线；修正右侧段落换行 | 23.08% → 0% |
| 5 | 请求箭头、分发曲线及标签原生化，仅保留中心齿轮小图标；修正英文换行 | 13.23% → 2.00% |
| 8 | 左侧窗口框架、导航、内容栏、控件和红叉原生化；右侧终端与转向箭头保留图片 | 26.36% → 13.75% |

第 12 页飞轮原生化尝试造成明显失真，已完整回退到前一版。第 8 页终端尝试存在叠字和细节损失，也回退对应区域，只接受左侧窗口拆解。未用强行移除图片替代视觉复核。

合并前后逐页比较结构化 IR，只有第 4、5、8 页发生变化；包括第 2 页和第 12 页在内的其他 13 页完全一致。

## 实际交付验证

产物位于本地 `output/component-native-batch2-20260907/`，上一版目录保留。包含完整 16 页 PPTX、PDF、结构化源归档和报告。新增 4 个原生文本框及 90 个原生形状/连接线；总计 221 个文本框、195 个其他原生形状、114 条连接线和 37 张局部图片。

直接检查 PPTX ZIP：16 个 slide XML、416 个 `p:sp`、114 个 `p:cxnSp`、37 个 `p:pic` 和 37 个媒体文件。PPTX SHA-256：`0157980e0b7b90bc1c82c40473212bd3b7ee4d6b01e8fbb815d8aca345a06c6b`。

逐页真实渲染并目视复核了接受的三个页面。整份真实 Worker handler 完成 16 页渲染、IR 校验、资源解析、原生图形门禁及多格式页数/源指纹一致性检查，上述检查均通过。

**视觉门禁仍未通过，不能称为保真度全面改善或完整生产验收通过。** 新增原生组件保留结构和文字，但失去部分源图纹理、发光及微小装饰，三个变更页的像素差异相较于保留大图的版本上升：第 4 页为 0.12899，第 5 页为 0.12471，第 8 页为 0.09712。整份报告比较 567 个组件区域，70 个关注区域，最终 `quality.passed=false`。

本批交付的是可编辑性进一步增强的复核版本，原版可用于视觉对照；仍需处理封面装饰、第 8 页终端、第 12 页飞轮及其他复杂局部图标。源样本已用于调试，不是独立验收集。未部署远程服务、更新已安装插件或推送 Git。
