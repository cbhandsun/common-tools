"use strict";

// Marker module for the KPI evidence adapter. Cache ownership lives here so
// font and geometry tuning can invalidate only the matching semantic page.
module.exports = {
  scope: "workflow-kpi-evidence"
};
